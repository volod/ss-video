"""Repository quality checks for the specification, plan, and documentation links.

Implementation lives in `ss_kit.quality`; this package re-exports it.
`make lint` uses the project interpreter (`.venv` when present), which must have ss-common
installed.
"""
